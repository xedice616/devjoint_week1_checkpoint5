spring:
  datasource:
    url: jdbc:postgresql://localhost:7878/library_management_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true